<!DOCTYPE html>
<html>
<head>
  <title>FilRouge</title>

  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= stylesheet_link_tag "slate", :media => "all" %>

  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">FilRouge SISI</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li class="active"> <a href="/slate">Home</a></li>
    </ul>

    <% if current_user %>
        <p class="navbar-text"><%= current_user.email %></p>

        <% if current_user.admin? %>
            <span class="label label-success navbar-text">Admin</span>
        <% else %>
            <span class="label label-warning navbar-text">not Admin</span>
        <% end %>

        <ul class="nav navbar-nav navbar-right">
          <%= form_tag destroy_user_session_path, method: :delete do %>

              <button type="submit" class="btn btn-danger navbar-btn">Déconnexion</button>
          <% end %>
    <% else %>
        <%= link_to "Connexion", new_user_session_path, class: "btn btn-success navbar-btn navbar-right" %>

    <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>




<div class="container"  >
  <%= yield %>
</div>

</body>
</html>
